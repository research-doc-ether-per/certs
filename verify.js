
取得した presentation_definition において、タイプの検索方法に正規表現の部分一致が使用されているため、フォーマット（format）が一致する証明書を検索した際に、タイプが部分一致する別の証明書まで誤って抽出されてしまいます。

presentation_definition 内の判定条件を動的に解析し、値が完全に一致することを保証する const（完全一致）キーワード へ置換する処理を実装します。

取得した定義のパス（path）を判別し、形式に応じて以下のように条件を出し分けます。

JWT-VC の場合（指定タイプがJWTの場合）：
検索パスに $.vc.type が含まれているかを判別します。該当する場合、従来の pattern による部分一致を廃止し、JSON Schema の contains と const を組み合わせた条件 に書き換えることで、配列内のタイプ名との「完全一致」へと変更します。

SD-JWT の場合：
検索パスに $.vct が含まれているかを判別します。該当する場合、pattern を廃止して type: "string" と const を直接指定する条件 に書き換えることで、VCT文字列の「完全一致」へと変更します。
