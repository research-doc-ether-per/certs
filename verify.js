
# 検証者（Verifier）を一意に識別するためのクライアントID
# (OpenID4VP リクエストの client_id として使用されます)

# ウォレットの画面上に表示される検証者のメタデータ（名称やロゴ）
clientMetadata: {
    # ウォレットに表示される検証者の名前
    # ウォレットに表示される検証者のロゴ画像のURL
}

# 検証セッションを開始・処理するためのベースURLプレフィックス
# (ウォレットが検証リクエストを取得したり、結果をポストする宛先になります)

# ウォレットアプリをディープリンクで起動、または認可を開始するためのベーススキーム/URL

# [任意] 検証者がリクエストに署名する際などに使用する、検証者自身の秘密鍵・公開鍵ペア (JWK形式)

# [任意] 上記の鍵に対応する X.509 証明書チェーン (Base64エンコードされたDER形式のリスト)
# (ウォレット側が「信頼できる検証者ドメインか」を検証するために使用されます)


# did:web 方式の解決（Resolver）において、HTTPS を強制するかどうか
# true : 規格通り HTTPS 通信のみを許可します（本番環境用）
# false: ローカルテスト等のため、非暗号化の HTTP 通信も許可します（開発モード用）


# 有効化する機能（フィーチャー）のリスト
# (ここに記載された機能モジュールがサービス起動時にロードされます)

# 無効化する機能（フィーチャー）のリスト
# (明示的にオフにしたい機能モジュールをここに指定します)
